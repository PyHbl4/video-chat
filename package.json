{
  "name": "self-hosted-videochat",
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:python": "docker compose -f infra/docker/docker-compose.dev.yml up --build",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "turbo run format",
    "gen:contracts": "pnpm -C packages/contracts run gen",
    "docker:dev": "docker compose -f infra/docker/docker-compose.dev.yml up --build"
  },
  "devDependencies": {
    "eslint": "^9.38.0",
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "^5.9.3"
  }
}
