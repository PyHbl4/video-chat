{
  "name": "@video-chat/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn videochat_api.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn videochat_api.main:app --host 0.0.0.0 --port 8000",
    "lint": "echo 'Python linting handled via Ruff in future stage'",
    "format": "echo 'Python formatting handled via Ruff in future stage'",
    "typecheck": "echo 'Python typechecking handled via MyPy in future stage'"
  }
}
