---
title: "Web: Экран 1:1 звонка"
status: backlog
assignee: "codex"
priority: high
eta: null
tags: [frontend, web, stage3, webrtc]
links: ["docs/codex/WEB-CLIENT-TODO.md", "docs/codex/SIGNALING-WEBRTC-TODO.md"]
---

## Контекст

Для Stage 3 нужно реализовать `/call/[roomId]` с медиагридом, mute/unmute, выбор устройств и интеграцией WebRTC (mesh). Текущий фронт не имеет ни устройства, ни хуков для WebRTC.

## Что сделать

- [ ] Построить хуки `usePeer`/`useCallRoom` для управления RTCPeerConnection, трекингом stream'ов и ICE обменом через socket.io события.
- [ ] Добавить UI: основной видео-поток, плавающие плитки, тулбар (mute, camera, screen share, hang up), incoming call toast.
- [ ] Реализовать pre-call проверку устройств (камера/микрофон) и обработку ошибок разрешений.
- [ ] Написать e2e smoke (два браузера через Playwright + fake медиа), unit-тесты для хуков (используя webrtc-mock).

## Заметки

Обеспечить graceful degradation (fallback на аудио), показывать состояние подключения/переподключения.

## История выполнения

Еще не выполнялась.
