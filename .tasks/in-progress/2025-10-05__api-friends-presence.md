---
title: "API: Friends, requests и presence"
status: in_progress
assignee: "codex"
priority: high
eta: null
tags: [api, stage2, websocket]
links: ["docs/codex/API-SPEC-TODO.md", "docs/codex/SIGNALING-WEBRTC-TODO.md"]
---

## Контекст

В Stage 2 требуется дружба и presence: таблица `friends`, REST API для запросов/акцепта, а также online-статусы через Redis + socket.io. Сейчас в API нет ни моделей, ни WS-событий.

## Что сделать

- [ ] Добавить модели и миграцию для `friends` (статусы requested/accepted/blocked) и сервис слой для проверок взаимности.
- [ ] Реализовать эндпоинты `/friends/request`, `/friends/accept`, `/friends/decline`, `/friends` и поиск `/users/search`.
- [ ] Настроить Redis presence: хранение последней активности, TTL, отдельные каналы socket.io `presence:update`.
- [ ] Реализовать socket.io namespacing (`user:{id}`) и события при изменении дружбы/онлайн-статуса.
- [ ] Покрыть тестами REST и базовый WS-флоу (например, через `async_client` + fake Redis).

## Заметки

Учесть rate-limit отправки заявок и проверку блокировок. БД индексы: `(user_id, status)`, `(friend_id, status)`.

## История выполнения

### 1. 2025-10-06 09:00 (GMT + 2). Аудит прогресса и синхронизация статуса

- Что хотим сделать и для чего (если причина - наш прошлый баг, описать его причину):
  - Проанализировать репозиторий, чтобы понять, какой объём работы уже выполнен по задаче друзей/presence и зафиксировать фактический статус в трекере.
- Как мы это реализуем:
  - Проверили фронтенд (`apps/web`) и пакеты (`packages/contracts`) — обнаружили готовые стора, контрактный слой и UI для страницы `/app`, но отсутствие бэкенд-части (`/friends/*`, модели и миграции).
- Как мы убедимся, что это работает и не конфликтует с другими решениями:
  - Зафиксируем результат в трекере задачи, оставив чек-лист неизменным, так как бэкенд-функциональность ещё не реализована.

### 2. 2025-10-06 09:20 (GMT + 2). Фикс UI-ошибки из-за отсутствующего Avatar

- Что хотим сделать и для чего (если причина - наш прошлый баг, описать его причину):
  - Исправить runtime-ошибку страницы `/app`, возникающую из-за того, что компонент `Avatar` импортируется из UI-пакета, но не экспортируется/не реализован.
- Как мы это реализуем:
  - Добавим в `@video-chat/ui` доступный для импорта компонент `Avatar` на основе базовых HTML-элементов и экспортируем его.
- Как мы убедимся, что это работает и не конфликтует с другими решениями:
  - Прогоним `pnpm --filter @video-chat/web lint`, чтобы убедиться, что импорт больше не undefined, и проконтролируем сборку на следующих запусках `pnpm dev`/CI.
